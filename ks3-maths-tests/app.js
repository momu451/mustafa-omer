(()=>{'use strict';const S={bank:null,paper:null,answers:{},timer:null,left:1800,marked:false};const $=id=>document.getElementById(id);const E={timer:$('timer'),paperSelect:$('paperSelect'),startBtn:$('startBtn'),randomBtn:$('randomBtn'),statusPanel:$('statusPanel'),paperMeta:$('paperMeta'),paperTitle:$('paperTitle'),progressText:$('progressText'),marksText:$('marksText'),form:$('paperForm'),actions:$('actions'),submitBtn:$('submitBtn'),printBtn:$('printBtn'),resetBtn:$('resetBtn'),result:$('result'),historyList:$('historyList')};const esc=v=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');const fmt=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;function stop(){if(S.timer){clearInterval(S.timer);S.timer=null}}function startTimer(mins){stop();S.left=mins*60;E.timer.textContent=fmt(S.left);S.timer=setInterval(()=>{S.left=Math.max(0,S.left-1);E.timer.textContent=fmt(S.left);if(S.left===0){stop();mark()}},1000)}function normalise(raw){if(raw&&raw.generator&&window.KS3GenerateBank)return window.KS3GenerateBank(raw);return raw}function getHist(){try{return JSON.parse(localStorage.getItem('ks3MathsScores')||'[]')}catch{return[]}}function saveHist(x){const h=getHist();h.unshift(x);localStorage.setItem('ks3MathsScores',JSON.stringify(h.slice(0,20)));renderHist()}function renderHist(){const h=getHist();E.historyList.innerHTML=h.length?h.map(x=>`<div class="historyItem"><span>${esc(x.paperTitle)}</span><strong>${x.score}/${x.total} (${x.percent}%)</strong></div>`).join(''):'No scores yet.'}function populate(){E.paperSelect.innerHTML=S.bank.papers.map((p,i)=>`<option value="${i}">${esc(p.paperTitle)}</option>`).join('')}function progress(){if(!S.paper)return;E.progressText.textContent=`${Object.keys(S.answers).length} / ${S.paper.questions.length} answered`}function selectVisual(name){E.form.querySelectorAll(`input[name="${name}"]`).forEach(i=>i.closest('.option').classList.toggle('selected',i.checked))}function renderPaper(i){stop();S.paper=S.bank.papers[i];S.answers={};S.marked=false;E.paperSelect.value=String(i);E.statusPanel.hidden=false;E.form.hidden=false;E.actions.hidden=false;E.result.hidden=true;E.result.innerHTML='';E.paperMeta.textContent=`${S.paper.questions.length} questions · ${S.paper.totalMarks} marks · ${S.paper.timeMinutes} minutes`;E.paperTitle.textContent=S.paper.paperTitle;E.marksText.textContent=`${S.paper.totalMarks} marks`;E.form.innerHTML=S.paper.questions.map(q=>`<fieldset class="question"><div class="questionHead"><div><div class="qid">Question ${q.id}</div><div class="topic">${esc(q.topic)}</div></div><span class="badge">${q.marks} mark${q.marks===1?'':'s'}</span></div><legend class="questionText">${esc(q.questionText)}</legend><div class="options">${q.options.map(o=>`<label class="option"><input type="radio" name="q-${q.id}" value="${esc(o.label)}"><span class="optionLetter">${esc(o.label)}</span><span class="optionValue">${esc(o.value)}</span></label>`).join('')}</div></fieldset>`).join('');E.form.querySelectorAll('input[type="radio"]').forEach(input=>input.addEventListener('change',()=>{S.answers[input.name.replace('q-','')]=input.value;selectVisual(input.name);progress()}));progress();startTimer(S.paper.timeMinutes);window.scrollTo({top:0,behavior:'smooth'})}function mark(){if(!S.paper||S.marked)return;let score=0;const review=S.paper.questions.map(q=>{const selected=S.answers[String(q.id)]||null;const correct=selected===q.correctAnswer;if(correct)score+=q.marks;return{q,selected,correct}});const total=S.paper.totalMarks,percent=Math.round(score/total*100);S.marked=true;stop();E.result.hidden=false;E.result.innerHTML=`<p class="eyebrow">Marked result</p><div class="scoreBig">${score}/${total}</div><h2>${percent}%</h2><p>${review.filter(r=>r.correct).length} of ${review.length} questions correct. Repair every wrong topic before another mixed paper.</p><div class="reviewList">${review.map(r=>`<div class="reviewItem ${r.correct?'correct':''}"><strong>Q${r.q.id}: ${r.correct?'Correct':'Wrong'}</strong><p>${esc(r.q.questionText)}</p><p>Your answer: <strong>${esc(r.selected||'not answered')}</strong></p><p>Correct answer: <strong>${esc(r.q.correctAnswer)} — ${esc(r.q.correctValue)}</strong></p><p>Topic: ${esc(r.q.topic)} · ${r.q.marks} mark${r.q.marks===1?'':'s'}</p></div>`).join('')}</div>`;saveHist({paperTitle:S.paper.paperTitle,score,total,percent,when:new Date().toISOString()});E.result.scrollIntoView({behavior:'smooth',block:'start'})}function reset(){if(!S.paper)return;S.answers={};S.marked=false;E.form.reset();E.form.querySelectorAll('.option').forEach(x=>x.classList.remove('selected'));E.result.hidden=true;progress();startTimer(S.paper.timeMinutes)}async function boot(){try{let raw;try{const r=await fetch('./question-bank.json',{cache:'no-store'});if(!r.ok)throw new Error(r.status);raw=await r.json()}catch(e){raw={generator:'KS3GenerateBank'}}S.bank=normalise(raw);if(!S.bank||!S.bank.papers||S.bank.papers.length!==35)throw new Error('Question bank did not initialise correctly.');populate();renderHist();E.startBtn.onclick=()=>renderPaper(Number(E.paperSelect.value));E.randomBtn.onclick=()=>renderPaper(Math.floor(Math.random()*S.bank.papers.length));E.submitBtn.onclick=mark;E.printBtn.onclick=()=>window.print();E.resetBtn.onclick=reset;if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js').catch(()=>{})}catch(e){document.body.innerHTML=`<main class="shell"><section class="panel"><h1>App failed to load</h1><p>${esc(e.message)}</p></section></main>`}}boot()})();
